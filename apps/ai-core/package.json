{
  "name": "@opensign/ai-core",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --host 0.0.0.0 --port 5000 --reload",
    "start": "uvicorn src.main:app --host 0.0.0.0 --port 5000",
    "typecheck": "echo 'No typecheck for Python'",
    "lint": "ruff check src",
    "test": "pytest"
  }
}
