{
  "name": "@opensign/api-core",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "typecheck": "echo 'No typecheck step configured for Python'",
    "lint": "ruff check .",
    "test": "pytest"
  }
}
